doctype html
html
    include includes/head.pug
    body 
        if products.length
            table.table
                thead
                    tr
                    th(scope='col') Product name
                    th(scope='col') Price
                    th(scope='col') Thumbnail
                tbody.table-group-divider
                    each item in products
                        tr
                            td= item.title
                            td= item.price
                            td
                                img(src=item.thumbnail, width="50")
        else
            p No existen productos
        a.btn.btn-success(href='/') Crear un nuevo producto 
        include includes/foot.pug
